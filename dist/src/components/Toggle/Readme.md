Toggle on example:

```js
<Toggle
  id="toggle"
  handleChange={() => (alert('change click'))}
  checked={true}
/>
```


Toggle off example:

```js
<Toggle
  id="toggle"
  handleChange={() => (alert('change click'))}
  checked={false}
/>
```